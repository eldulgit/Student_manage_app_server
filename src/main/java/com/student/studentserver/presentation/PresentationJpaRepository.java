package com.student.studentserver.presentation;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PresentationJpaRepository extends JpaRepository<PresentationEntity,Integer> {
}
